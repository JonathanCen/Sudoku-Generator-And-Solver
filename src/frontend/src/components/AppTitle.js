import React from 'react';
import Typography from "@mui/material/Typography";

function AppTitle() {
    return (
        <Typography component="h4" variant="h4" id="appTitle">Sudoku Solver</Typography>
    )
}

export default AppTitle